let val;

val = window.outerHeight;
val = window.outerHeight;

val = window.innerHeight;
val = window.innerWidth;

val = window.scrollY;
val = window.scrollX;

val = window.location; //현재 페이지의 정보
// window.location.href = 'https://naver.com';

val = window.history.back();
val = window.history.forward()

// window.navigator();

console.log(val);